[pytest]
# src/ has no __init__.py and its modules import each other with flat names
# (e.g. "from extract import ...", not "from src.extract import ..."), so
# tests need src/ on sys.path rather than importing it as a package.
pythonpath = src
